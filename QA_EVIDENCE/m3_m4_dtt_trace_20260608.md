# M3/M4 DTT Trace - ConceptMaster

Generated: 2026-06-08

## M3 Visible Evidence Screen

Definition:
The first screen must visibly show the judge-facing learning loop: wrong-answer DNA map, AI diagnosis card, same-concept retry card, and before-after change card.

Test:
- `tests/concept-master.test.mjs::M3 visible evidence cards expose diagnosis, retry, and before-after proof`
- Required DOM ids:
  - `misconception-map-panel`
  - `ai-diagnosis-card`
  - `same-concept-retry-card`
  - `before-after-card`
  - `diagnosis-card-title`
  - `retry-card-title`
  - `before-mistake-count`
  - `after-mistake-count`

Trace:
- `index.html`: visible cards are in the first evidence grid, not inside `system-hidden`.
- `src/app.js`: `renderVisibleEvidenceCards()` updates AI diagnosis, retry recommendation, and before-after mistake counts from learning events.
- `styles.css`: `.judge-flow-card` and `.before-after-metrics` keep cards readable inside Geonho's existing visual shell.

## M4 30-Second Fallback-Safe Demo

Definition:
The judge demo must complete without live Manus API within 30 seconds by using rule-based diagnosis fallback and template same-concept retry fallback.

Test:
- `tests/concept-master.test.mjs::M4 judge demo contract is fallback-safe under 30 seconds without Manus API`
- Contract:
  - max duration: `11450ms`
  - required max duration: `30000ms`
  - diagnosis fallback: `rule_based_fallback`
  - generation fallback: `template_fallback`
  - dataSource: `judge_demo`
  - retryResult: `retried_then_cleared`

Trace:
- `src/demoContract.js`: explicit demo timing and fallback contract.
- `src/app.js::runJudgeDemo`: uses `DEMO_API_TIMEOUT_MS`, `judge_demo`, `template_fallback`, and `retried_then_cleared`.
- `npm test`: 36 PASS.

## Browser Trace Boundary

Browser screenshot trace is not attached in this file.

Reason:
- Browser MCP route previously failed because the shared MCP Chrome profile was already in use.
- node_repl Playwright route failed because bundled `playwright` could not resolve `playwright-core`.

Next safe action:
Repair or isolate the Browser/Playwright runtime once, then capture a screenshot of `http://localhost:4173/` showing the four visible evidence cards.
