# M5-M6 DTT Trace

Date: 2026-06-08

## Definition

M5: The data screen must show real learning-log evidence, not only dashboard copy. It must expose `demo_seed`, `judge_demo`, and `human_trial` as separate sources and render both `attempt_log` and `concept_summary`.

Historical M6 draft at this checkpoint: The submission package must explain the same product message as the app: elementary grade-6 math wrong-answer DNA map, AI/fallback diagnosis, same-concept retry, review schedule, data evidence, 30-second demo, and 2-minute presentation script.

Current M6 definition after the 2026-06-09 handoff update: Human-Verified requires an actual Geonho or owner trial record validated through `scripts/validate_human_trial_record.py`; submission files alone are not M6 completion evidence.

## Test

- `tests/concept-master.test.mjs::M5 learning evidence separates demo_seed, judge_demo, and human_trial logs on the data screen`
- `tests/concept-master.test.mjs::CodeFair submission package includes summary, architecture, 2-minute script, judge Q&A, and M5 evidence`
- `tests/concept-master.test.mjs::presentation plan anchors a 2-minute script to elementary math representative problems`
- `tests/concept-master.test.mjs::M4 judge demo contract is fallback-safe under 30 seconds without Manus API`

## Trace

- `src/misconceptionMap.js::buildLearningEvidencePayload`
  - schema: `concept_master_m5_learning_evidence.v1`
  - outputs: `attempt_log`, `concept_summary`, `dataSources`
  - source separation: `demo_seed`, `judge_demo`, `human_trial`
- `index.html`
  - `learning-log-evidence-panel`
  - `data-source-summary`
  - `attempt-log-preview`
  - `concept-summary-evidence-panel`
  - `concept-summary-preview`
  - `learning-log-json`
- `src/app.js::renderDataEvidence`
  - renders source count pills
  - renders bounded `attempt_log` rows
  - renders `concept_summary` rows
  - renders JSON evidence block
- `src/presentationPlan.js`
  - 2-minute cue cards total 120 seconds
  - representative problems stay elementary math
- `src/demoContract.js`
  - 30-second demo contract remains fallback-safe without Manus API
- `SUBMISSION_PACKAGE.md`
  - includes 작품요약, 기술 구조도, 30초 시연 흐름, 2분 영상 대본, 대표문항, 심사 Q&A, 한계와 개선계획

## Verification

- `npm test`: 38 PASS / 0 FAIL.
- HTTP smoke: `GET http://localhost:4173/` returned status 200.
- HTTP DOM evidence:
  - `attempt-log-preview`: present
  - `concept-summary-preview`: present
  - `learning-log-json`: present
  - `30초 데모`: present
- JS syntax checks:
  - `node --check src/app.js`: PASS
  - `node --check src/misconceptionMap.js`: PASS
  - `node --check src/presentationPlan.js`: PASS
  - `node --check src/dttTrace.js`: PASS
  - `node --check src/readinessAudit.js`: PASS
- stale text scan:
  - no current matches for `36 PASS`, `32 PASS`, `90초`, `90-second`, `22 Korean`, `math, science`, or `science, and English` in current core files.

## Browser Boundary

The Codex in-app Browser MCP route is still blocked by the existing shared Chrome profile lock:

```text
Browser is already in use for C:\Users\pjmin\AppData\Local\ms-playwright\mcp-chrome-41da846
```

Per the stop rule, the same browser route was not retried again. Current browser-grade evidence is HTTP/DOM verification only; a visual screenshot remains pending until the Browser MCP profile is isolated or released.

## Status

- M5: Auto-Verified by tests and HTTP DOM evidence.
- Historical submission-package draft: Auto-Verified by tests and file evidence at this checkpoint.
- Current M6 human-trial gate: pending until `GEONHO_HUMAN_TRIAL_CHECKLIST.md` and a copied `trial_records/*.json` record are filled after actual use and validated in `trial` mode.
- Human-Verified: still false until `GEONHO_HUMAN_TRIAL_CHECKLIST.md` is actually run and filled.
